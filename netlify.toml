[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

# Proxy API requests to backend (if configured) or fallback to React Router SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
